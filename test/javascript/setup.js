// Global Vitest setup for the Stimulus controller specs. jsdom already provides
// everything the current controllers touch (classList, dataset, replaceChildren,
// createElement/createTextNode), so no polyfills are needed yet — add them here
// if a controller starts using a browser API jsdom doesn't implement.
