<section class="pn-wt-section pn-wt-maps">
  <% loc.area_maps.each_with_index do |area, index| %>
    <%= render "area_map_block", loc: loc, area: area, show_howto: index.zero? %>
  <% end %>
</section>
