<div class="pn-account__panel">
  <div class="pn-account__panel-head">
    <span class="pn-account__panel-title"><%= title %></span>
    <span class="pn-account__panel-meta"><%= meta %></span>
  </div>
  <div class="pn-account__panel-body"><%= yield %></div>
</div>
